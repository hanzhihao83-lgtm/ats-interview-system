import type { Candidate, DuplicateCandidateResult } from "../types/recruitment";
export function detectDuplicateCandidate(target:Candidate, all:Candidate[]):DuplicateCandidateResult { const matched=all.filter(c=>c.id!==target.id); const exact=matched.filter(c=>c.phone===target.phone || (!!target.idCardMasked&&c.idCardMasked===target.idCardMasked) || (c.name===target.name&&c.phone.slice(-4)===target.phone.slice(-4))); const suspected=matched.filter(c=>!exact.includes(c)&&((c.name===target.name&&c.position===target.position)||(c.name===target.name&&c.vendor!==target.vendor)||(c.phone.slice(-4)===target.phone.slice(-4)&&c.position===target.position))); if(exact.length) return {isDuplicate:true,level:"确定重复",matchedCandidateIds:exact.map(c=>c.id),reasons:["手机号或姓名+手机号后四位一致"]}; if(suspected.length) return {isDuplicate:true,level:"疑似重复",matchedCandidateIds:suspected.map(c=>c.id),reasons:["姓名、岗位或手机号后四位存在相似"]}; return {isDuplicate:false,level:"无",matchedCandidateIds:[],reasons:[]}; }
