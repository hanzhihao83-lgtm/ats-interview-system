import dayjs from "dayjs"; import type { Candidate } from "../types/recruitment";
export type TodoType="简历筛选"|"安排面试"|"面试反馈"|"确认Offer"|"人员报到"|"供应商催办"|"培训确认"|"重复处理";
export interface RecruitmentTodo{id:string;candidateId:string;type:TodoType;title:string;description:string;priority:"高"|"中"|"低";deadline?:string;owner:string;completed:boolean;generatedByRule:boolean}
export function generateRecruitmentTodos(rows:Candidate[],date:string):RecruitmentTodo[]{const todos:RecruitmentTodo[]=[]; rows.forEach(c=>{if(c.currentStatus==="简历待筛选")todos.push({id:`todo-screen-${c.id}`,candidateId:c.id,type:"简历筛选",title:`筛选${c.name}的简历`,description:"候选人简历等待初筛。",priority:"中",owner:"招聘专员",completed:false,generatedByRule:true}); if(c.currentStatus==="待面试")todos.push({id:`todo-interview-${c.id}`,candidateId:c.id,type:"安排面试",title:`安排${c.name}面试`,description:"候选人已通过简历筛选，等待安排面试。",priority:"中",owner:"招聘专员",completed:false,generatedByRule:true}); if(c.interviewResult==="待反馈"&&c.interviewDate&&dayjs(date).diff(dayjs(c.interviewDate),"hour")>24)todos.push({id:`todo-feedback-${c.id}`,candidateId:c.id,type:"面试反馈",title:`跟进${c.name}面试反馈`,description:`面试结束超过24小时，结果仍未反馈。`,priority:"高",owner:c.interviewer||"Kim",deadline:date,completed:false,generatedByRule:true}); if(c.currentStatus==="待入职"&&c.expectedEntryDate&&c.expectedEntryDate<=date)todos.push({id:`todo-entry-${c.id}`,candidateId:c.id,type:"人员报到",title:`确认${c.name}是否报到`,description:"预计入职日期已到或逾期，尚未完成报到。",priority:"高",owner:"供应商HR",completed:false,generatedByRule:true}); if(c.duplicateCheck?.isDuplicate)todos.push({id:`todo-dup-${c.id}`,candidateId:c.id,type:"重复处理",title:`处理${c.name}重复风险`,description:"候选人可能由多个供应商重复提交。",priority:"高",owner:"招聘专员",completed:false,generatedByRule:true});}); return todos;}
