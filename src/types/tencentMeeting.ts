export type InterviewStatus = "待创建会议" | "会议创建中" | "已安排" | "待面试" | "进行中" | "待反馈" | "已完成" | "已取消" | "未到场" | "会议创建失败";
export type InterviewResult = "待反馈" | "通过" | "不通过" | "待复试" | "未到场" | "取消";
export interface TencentMeetingInfo { meetingId: string; meetingCode?: string; subject: string; joinUrl?: string; hostJoinUrl?: string; password?: string; scheduledStartTime: string; scheduledEndTime: string; status: "scheduled" | "started" | "ended" | "cancelled" | "unknown"; providerCreatedAt?: string; rawResponseDigest?: string }
export interface InterviewParticipantRecord { id: string; interviewId: string; participantName: string; participantRole: "候选人" | "面试官" | "招聘人员" | "其他"; joinTime?: string; leaveTime?: string; durationSeconds: number; joinCount: number; matchedAutomatically: boolean; matchConfidence?: number; manuallyConfirmed?: boolean }
export interface TencentMeetingRecording { recordingId: string; meetingId: string; recordType: "云录制" | "文字转写" | "视频录制" | "音频录制" | "未知"; title?: string; status: "处理中" | "可用" | "失败" | "未知"; startTime?: string; endTime?: string; durationSeconds?: number; playUrl?: string; shareUrl?: string; transcriptAvailable: boolean; minutesAvailable: boolean; syncedAt: string }
