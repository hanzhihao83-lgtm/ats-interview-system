import type { Candidate } from "./recruitment";
export type ImportValidationStatus = "待校验" | "校验通过" | "警告" | "校验失败";
export type ImportAction = "跳过" | "创建为新候选人" | "更新已有候选人" | "合并数据" | "待人工复核";
export interface CandidateImportRow { rowNumber:number; name?:string; phone?:string; email?:string; vendor?:string; project?:string; position?:string; university?:string; major?:string; highestEducation?:string; graduationYear?:number; isFreshGraduate?:boolean; resumeSubmitDate?:string; resumeResult?:string; currentStatus?:string; interviewDate?:string; interviewRound?:string; interviewResult?:string; interviewer?:string; expectedEntryDate?:string; actualEntryDate?:string; remark?:string; updatedBy?:string; validationStatus:ImportValidationStatus; errors:string[]; warnings:string[]; duplicate?:ImportDuplicateResult; action:ImportAction }
export interface ImportDuplicateResult { level:"无重复"|"同名不同人"|"疑似重复"|"高度疑似重复"|"确定重复"|"需人工复核"; matchedCandidateIds:string[]; matchedImportRows:number[]; reasons:string[] }
export interface CandidateImportTask { id:string; fileName:string; sheetName:string; totalRows:number; validRows:number; warningRows:number; invalidRows:number; duplicateRows:number; importedRows:number; skippedRows:number; failedRows:number; status:"文件已上传"|"解析中"|"待映射"|"校验中"|"待确认"|"导入中"|"导入完成"|"部分失败"|"导入失败"; uploadedBy:string; uploadedAt:string; importedAt?:string }
export interface ImportContext { vendors:string[]; positions:string[]; projects:string[]; statuses:string[]; existing:Candidate[]; batch:CandidateImportRow[] }
