import { Card } from "antd"; import ReactECharts from "echarts-for-react";
export default function SupplierResultChart({ data }: { data: any[] }) { return <Card title="供应商结果对比"><ReactECharts style={{ height: 360 }} option={{ tooltip: { trigger: "axis" }, legend: {}, dataZoom: data.length > 8 ? [{ type: "slider" }] : [], xAxis: { type: "category", data: data.map((x) => x.supplier) }, yAxis: { type: "value", minInterval: 1 }, series: [["candidates", "候选人"], ["passed", "通过"], ["joined", "已入职"], ["abandoned", "已放弃"]].map(([field, name]) => ({ name, type: "bar", data: data.map((x) => x[field]) })) }} /></Card>; }
