import { Card } from "antd"; import ReactECharts from "echarts-for-react";
export default function RecruitmentFunnel({ data }: { data: any[] }) { return <Card title="招聘转化漏斗"><ReactECharts style={{ height: 330 }} option={{ tooltip: { trigger: "item", formatter: (p: any) => `${p.name}<br/>人数：${p.value}<br/>上一阶段：${p.data.previousRate == null ? "—" : `${(p.data.previousRate * 100).toFixed(1)}%`}<br/>累计：${p.data.cumulativeRate == null ? "—" : `${(p.data.cumulativeRate * 100).toFixed(1)}%`}` }, series: [{ type: "funnel", label: { formatter: (p: any) => `${p.name}  ${p.value}` }, data: data.map((x) => ({ ...x, value: x.count })) }] }} /></Card>; }
