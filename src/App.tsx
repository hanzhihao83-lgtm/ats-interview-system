import { Component, useEffect, useMemo, useState, type ReactNode } from "react";
import { Alert, Badge, Button, Card, DatePicker, Descriptions, Divider, Drawer, Empty, Form, Input, Layout, List, Modal, Progress, Select, Space, Statistic, Table, Tabs, Tag, Typography, message } from "antd";
import { CopyOutlined, DownloadOutlined, FileSearchOutlined, FileTextOutlined, UploadOutlined, ReloadOutlined, SearchOutlined } from "@ant-design/icons";
import dayjs, { Dayjs } from "dayjs";
import ReactECharts from "echarts-for-react";
import type { Candidate, CandidateStatus, OperationLog } from "./types/recruitment";
import { candidates } from "./data/candidates";
import { allowedStatusTransitions, canTransitionStatus } from "./config/statusFlow";
import { getCandidateStatusAtDate } from "./utils/historicalStatus";
import { calculateFunnelData, calculateOverviewMetrics, detectRisks, filterCandidates, maskPhone } from "./utils/statistics";
import { detectDuplicateCandidate } from "./utils/duplicateDetection";
import { generateRecruitmentTodos, type RecruitmentTodo } from "./utils/todoGenerator";
import { validateCandidateConsistency } from "./utils/consistencyValidation";
import { calculateVendorPerformance } from "./utils/vendorPerformance";
import { generateDailyReport } from "./utils/report";
import { exportDashboardExcel } from "./utils/exportExcel";
import ResumeScreeningPage from "./pages/ResumeScreeningPage";
import InterviewBookingButton from "./components/InterviewBookingButton";
import CandidateImportPage from "./pages/CandidateImportPage";

class ImportPageErrorBoundary extends Component<{ children: ReactNode }, { error: Error | null }> {
  state = { error: null as Error | null };
  static getDerivedStateFromError(error: Error) { return { error }; }
  componentDidCatch(error: Error) { console.error("PDF 简历导入页面渲染失败", error); }
  render() {
    if (this.state.error) return <div className="content import-page"><Alert type="error" showIcon message="PDF 简历导入页面加载失败" description={`请刷新页面后重试：${this.state.error.message || "未知错误"}`} /></div>;
    return this.props.children;
  }
}

const { Header, Content } = Layout;
const vendors=["全部供应商","人瑞","供应商B","供应商C","供应商D"];
const positions=["全部岗位","AI 数据标注员","AI 数据质检员","视频评测工程师","Caption 标注员","项目助理"];
const statuses:CandidateStatus[]=["简历待筛选","简历未通过","待安排面试","待面试","面试待反馈","面试未通过","面试通过","待确认入职","待入职","培训中","培训未通过","项目中","候选人放弃","已离职","异常"];
const statusColor:Record<string,string>={项目中:"green",培训中:"gold",培训未通过:"red",待入职:"purple",待面试:"blue",面试待反馈:"orange",面试通过:"cyan",面试未通过:"red",简历未通过:"red",候选人放弃:"default",异常:"red",简历待筛选:"default",待安排面试:"geekblue",待确认入职:"purple",已离职:"default"};

export default function App(){
  const [date,setDate]=useState<Dayjs>(dayjs("2026-08-01")); const [vendor,setVendor]=useState("全部供应商"); const [statusFilter,setStatusFilter]=useState<string[]>([]); const [position,setPosition]=useState("全部岗位"); const [search,setSearch]=useState(""); const [rows,setRows]=useState<Candidate[]>(candidates); const [selected,setSelected]=useState<Candidate|null>(null); const [reportOpen,setReportOpen]=useState(false); const [performanceVendor,setPerformanceVendor]=useState<string|null>(null); const [statusOpen,setStatusOpen]=useState(false); const [statusForm]=Form.useForm(); const [todoFilter,setTodoFilter]=useState("全部"); const [completedTodos,setCompletedTodos]=useState<string[]>([]); const [screeningOpen,setScreeningOpen]=useState(false); const [importOpen,setImportOpen]=useState(false); const dateText=date.format("YYYY-MM-DD");
  if (screeningOpen) return <ResumeScreeningPage onBack={()=>setScreeningOpen(false)} onTransfer={(candidate)=>{setRows((list)=>list.some((item)=>item.id===candidate.id)?list:list.concat(candidate));setScreeningOpen(false);}} />;
  if (importOpen) return <ImportPageErrorBoundary><CandidateImportPage existing={rows} onBack={()=>setImportOpen(false)} onImport={(imported)=>{setRows((list)=>list.concat(imported));setImportOpen(false);message.success(`已导入${imported.length}名候选人，统计数据已刷新`);}} /></ImportPageErrorBoundary>;
  const filtered=filterCandidates(rows,dateText,vendor,statusFilter,position,search); const historicalRows=filtered.map(c=>({...c,currentStatus:getCandidateStatusAtDate(c,dateText)||c.currentStatus})); const metrics=calculateOverviewMetrics(historicalRows,dateText); const funnel=calculateFunnelData(historicalRows,dateText); const risks=detectRisks(historicalRows,dateText); const todos=generateRecruitmentTodos(historicalRows,dateText).filter(t=>todoFilter==="全部"||t.type===todoFilter).map(t=>({...t,completed:completedTodos.includes(t.id)})); const performances=calculateVendorPerformance(rows);
  const report=generateDailyReport(historicalRows,dateText); const reset=()=>{setVendor("全部供应商");setStatusFilter([]);setPosition("全部岗位");setSearch("")}; const refresh=()=>{setRows([...candidates]);message.success("数据已刷新")};
  const updateStatus=(values:{status:CandidateStatus;remark:string;date?:string})=>{ if(!selected||!values.remark.trim()) return message.error("状态变更必须填写备注"); const from=selected.currentStatus; if(!canTransitionStatus(from,values.status)) return message.error(`不允许从“${from}”直接变更为“${values.status}”`); const operatedAt=`${dateText} 12:00`; const event={id:`SE-${Date.now()}`,candidateId:selected.id,status:values.status,effectiveAt:values.date?`${values.date} 09:00`:operatedAt,operator:"招聘人员",source:"招聘人员" as const,remark:values.remark}; const log:OperationLog={id:`LOG-${Date.now()}`,candidateId:selected.id,module:"候选人",action:"修改候选人状态",field:"currentStatus",oldValue:from,newValue:values.status,operator:"招聘人员",operatedAt,reason:values.remark}; const next={...selected,currentStatus:values.status,statusEvents:[...selected.statusEvents,event],operationLogs:[...selected.operationLogs,log],timeline:[...selected.timeline,{time:operatedAt,action:`状态变更为${values.status}`,operator:"招聘人员",remark:values.remark}]}; setRows(list=>list.map(c=>c.id===next.id?next:c));setSelected(next);setStatusOpen(false);statusForm.resetFields();message.success("状态已更新并记录日志") };
  const toggleTodo=(todo:RecruitmentTodo)=>setCompletedTodos(list=>todo.completed?list.filter(id=>id!==todo.id):[...list,todo.id]);
  const riskTotal=Object.values(risks).reduce((sum,list)=>sum+list.length,0); const riskCards=[["面试结果待反馈",risks.feedback,"面试结束超过24小时，结果仍未反馈。"],["面试未到场",risks.absent,"候选人未按时参加面试，请供应商确认原因。"],["预计入职逾期",risks.overdue,"预计入职日期已过，候选人尚未完成报到。"],["入职延期",risks.delayed,"候选人预计入职日期发生延期。"],["候选人放弃",risks.giveup,"候选人已明确放弃本次机会。"],["供应商反馈超时",risks.vendor,"候选人处于待处理状态超过48小时。"]] as const;
  const funnelOption={tooltip:{trigger:"item",formatter:(p:{name:string;value:number})=>`${p.name}<br/>人数：${p.value}`},series:[{type:"funnel",left:"8%",top:8,bottom:8,width:"84%",min:0,max:Math.max(...funnel,1),minSize:"10%",maxSize:"100%",sort:"descending",gap:5,label:{show:true,position:"inside",color:"#fff",formatter:(p:{name:string;value:number})=>`${p.name} ${p.value}`},data:["提交简历","简历通过","已安排面试","面试通过","确认入职","实际入职"].map((name,i)=>({name,value:funnel[i]}))}]};
  const columns=[{title:"候选人",dataIndex:"name",render:(v:string,c:Candidate)=><a onClick={()=>setSelected(c)}>{v}</a>},{title:"联系方式",render:(_:unknown,c:Candidate)=>maskPhone(c.phone)},{title:"供应商",dataIndex:"vendor"},{title:"岗位",dataIndex:"position"},{title:"状态",dataIndex:"currentStatus",render:(v:string)=><Tag color={statusColor[v]}>{v}</Tag>},{title:"重复风险",render:(_:unknown,c:Candidate)=>c.duplicateCheck?.isDuplicate?<Tag color={c.duplicateCheck.level==="确定重复"?"red":"orange"} onClick={()=>message.info(c.duplicateCheck?.reasons.join("；"))}>{c.duplicateCheck.level}</Tag>:<span>—</span>},{title:"数据质量",render:(_:unknown,c:Candidate)=>{const issues=validateCandidateConsistency(c);return issues.length?<Tag color={issues.some(i=>i.level==="严重")?"red":"orange"}>{issues.length} 项问题</Tag>:<Tag color="green">正常</Tag>}},{title:"面试",render:(_:unknown,c:Candidate)=>`${c.interviews.length} 轮`},{title:"预计入职",dataIndex:"expectedEntryDate",render:(v?:string)=>v||"—"},{title:"更新时间",dataIndex:"updatedAt"}];
  return <Layout className="shell"><Header className="top-header"><div><Typography.Title level={3}>人员招聘与入职日报</Typography.Title><Typography.Text>统一追踪供应商简历、面试、入职和人员状态</Typography.Text></div><Space wrap><Button icon={<FileSearchOutlined/>} onClick={()=>setScreeningOpen(true)}>AI简历筛选</Button><Button icon={<UploadOutlined/>} onClick={()=>setImportOpen(true)}>Excel导入</Button><KimStatusPanel/><TencentMeetingStatusPanel/><Button icon={<ReloadOutlined/>} onClick={refresh}>刷新</Button><Button icon={<FileTextOutlined/>} onClick={()=>setReportOpen(true)}>预览日报</Button><Button icon={<CopyOutlined/>} onClick={()=>{navigator.clipboard?.writeText(report);message.success("日报已复制")}}>复制日报</Button><Button type="primary" icon={<DownloadOutlined/>} onClick={()=>exportDashboardExcel(historicalRows,dateText)}>导出 Excel</Button></Space></Header><Content className="content"><Card className="filter-card"><Space wrap><DatePicker value={date} onChange={v=>v&&setDate(v)} allowClear={false}/><Select value={vendor} options={vendors.map(v=>({label:v,value:v}))} onChange={setVendor} style={{width:140}}/><Select mode="multiple" allowClear placeholder="全部状态" value={statusFilter} options={statuses.map(v=>({label:v,value:v}))} onChange={setStatusFilter} style={{width:250}}/><Select value={position} options={positions.map(v=>({label:v,value:v}))} onChange={setPosition} style={{width:180}}/><Input prefix={<SearchOutlined/>} placeholder="搜索姓名、手机号后四位、供应商或岗位" value={search} onChange={e=>setSearch(e.target.value)} style={{width:280}}/><Button onClick={reset}>重置筛选</Button></Space></Card><div className="metric-grid">{[["项目中",metrics.project,"当前在岗"],["培训中",metrics.training,"已入职未完成培训"],["待入职",metrics.pending,"已确认 Offer"],["当日提交简历",metrics.submitted,"所选日期新增"],["当日面试通过",metrics.interviewPassed,"反馈日期命中"],["当日实际入职",metrics.joined,"完成报到"]].map(([name,value,desc])=><Card className="metric-card" key={name as string} onClick={()=>setStatusFilter([name as string])}><Statistic title={name as string} value={value as number}/><span>{desc as string}</span></Card>)}</div><Card title={<span>今日待办 <Badge count={todos.filter(t=>!t.completed).length}/></span>}><List dataSource={todos.slice(0,6)} locale={{emptyText:<Empty description="当前没有待处理任务"/>}} renderItem={todo=><List.Item actions={[<Button size="small" onClick={()=>toggleTodo(todo)}>{todo.completed?"已完成":"标记完成"}</Button>,<Button size="small" onClick={()=>setSelected(rows.find(c=>c.id===todo.candidateId)||null)}>查看</Button>]}><List.Item.Meta title={todo.title} description={todo.description}/></List.Item>}/></Card><div className="main-grid"><Card title="招聘转化漏斗"><ReactECharts option={funnelOption} style={{height:280}}/></Card><Card title="重点风险" extra={<Badge count={riskTotal}/>}><div className="risk-grid">{riskCards.map(([title,list,desc])=><div className="risk-item" key={title}><Badge count={list.length}/><strong>{title}</strong><p>{list.length?desc:"当前暂无风险"}</p></div>)}</div></Card></div><Card title="供应商绩效" extra={<span className="muted">简历到达数量 50% · 简历质量 40% · Offer 接收率 10%</span>}><Table rowKey="vendor" pagination={false} dataSource={performances} columns={[{title:"供应商",dataIndex:"vendor"},{title:"综合得分",dataIndex:"totalScore",render:(v:number,r:any)=><a onClick={()=>setPerformanceVendor(r.vendor)}>{v} 分</a>},{title:"评级",dataIndex:"level",render:(v:string)=><Tag>{v}</Tag>},{title:"简历到达",render:(_:unknown,r:any)=>`${r.metrics.resumeCount} 份`},{title:"简历质量",render:(_:unknown,r:any)=>r.metrics.resumePassRate===null?"—":`${r.metrics.resumePassRate}%`},{title:"Offer 接收率",render:(_:unknown,r:any)=>r.metrics.offerAcceptanceRate===null?"—":`${r.metrics.offerAcceptanceRate}%`}]} /></Card><Card title="候选人明细" extra={<span className="muted">当前筛选结果共 {filtered.length} 人</span>}><Table rowKey="id" columns={columns} dataSource={filtered} pagination={{pageSize:10,showTotal:t=>`共 ${t} 人`}} scroll={{x:1200}} /></Card></Content><Drawer title="候选人详情" width={520} open={Boolean(selected)} onClose={()=>setSelected(null)}>{selected&&<Descriptions column={1} bordered items={[{key:"name",label:"姓名",children:selected.name},{key:"phone",label:"手机号",children:maskPhone(selected.phone)},{key:"vendor",label:"供应商",children:selected.vendor},{key:"position",label:"岗位",children:selected.position},{key:"status",label:"当前状态",children:<Tag color={statusColor[selected.currentStatus]}>{selected.currentStatus}</Tag>}]} />}</Drawer><Modal title={`${dateText} 人员招聘与入职日报`} open={reportOpen} onCancel={()=>setReportOpen(false)} footer={null}><pre className="report">{report}</pre></Modal><Modal title="供应商绩效详情" open={Boolean(performanceVendor)} onCancel={()=>setPerformanceVendor(null)} footer={null}>{performanceVendor&&<VendorPerformanceView vendor={performances.find(v=>v.vendor===performanceVendor)!}/>}</Modal></Layout>;
}
function VendorPerformanceView({vendor}:{vendor:ReturnType<typeof calculateVendorPerformance>[number]}){const dims=[['简历到达数量',vendor.resumeVolumeScore],['简历质量',vendor.resumeQualityScore],['Offer 接收率',vendor.offerAcceptanceScore]];return <div><Space direction="vertical" style={{width:"100%"}}><Typography.Text type="secondary">本期共到达简历 {vendor.metrics.resumeCount} 份</Typography.Text>{dims.map(([name,value])=><div className="performance-row" key={name as string}><span>{name}</span><Progress percent={value as number} status={(value as number)<60?"exception":"active"}/></div>)}<Alert type={vendor.level==="高风险"?"error":"info"} message={`${vendor.vendor}本期综合评级为${vendor.level}，评分权重为简历到达数量50%、简历质量40%、Offer接收率10%。`}/></Space></div>}

function KimStatusPanel(){const [open,setOpen]=useState(false);const [status,setStatus]=useState<{configured:boolean;message:string}|null>(null);useEffect(()=>{fetch("/api/kim/status").then((res)=>res.json()).then(setStatus).catch(()=>setStatus({configured:false,message:"Kim服务未启动"}));},[]);return <><Button size="small" onClick={()=>setOpen(true)}>Kim提醒：{status?.configured?"已配置":"未配置"}</Button><Modal title="Kim机器人状态" open={open} onCancel={()=>setOpen(false)} footer={null}><Alert type={status?.configured?"success":"warning"} message={status?.message||"检测中"} description={status?.configured?"面试创建后将按提醒节点发送消息。":"请在服务端 .env 配置 KIM_WEBHOOK_URL；未配置时不影响面试数据保存。"}/><Divider/><Button onClick={()=>fetch("/api/interview-reminders/scan",{method:"POST"}).then(()=>message.success("已触发提醒扫描")).catch(()=>message.error("提醒服务未启动"))}>立即扫描提醒任务</Button></Modal></>}
function TencentMeetingStatusPanel(){const [open,setOpen]=useState(false);const [status,setStatus]=useState<{data?:{mode:string;message:string}}|null>(null);useEffect(()=>{fetch("/api/tencent-meeting/status").then((res)=>res.json()).then(setStatus).catch(()=>setStatus({data:{mode:"unknown",message:"会议服务未启动"}}));},[]);const mode=status?.data?.mode;return <><Button size="small" onClick={()=>setOpen(true)}>腾讯会议：{mode === "mock" ? "模拟模式" : mode === "api" ? "真实模式" : "未连接"}</Button><Modal title="腾讯会议连接状态" open={open} onCancel={()=>setOpen(false)} footer={null}><Alert type={mode === "mock" ? "info" : mode === "api" ? "success" : "warning"} message={status?.data?.message || "检测中"} description={mode === "mock" ? "当前不会创建真实会议，适合演示和联调。" : "真实 API 参数和鉴权需按腾讯会议官方文档配置。"}/><Divider/><Button onClick={()=>fetch("/api/tencent-meeting/test",{method:"POST"}).then((res)=>res.json()).then((body)=>message.info(body.data?.message||body.message||"已完成测试"))}>测试连接</Button></Modal></>}
