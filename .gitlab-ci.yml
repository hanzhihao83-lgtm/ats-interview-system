image: node:22

pages:
  stage: deploy
  script:
    - npm ci
    - npm run build
    - mv dist public
  pages:
    publish: public
  artifacts:
    paths:
      - public
  rules:
    - if: '$CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH'
